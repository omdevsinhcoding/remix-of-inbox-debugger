{
  "name": "email-cache-proxy",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "worker.js",
  "scripts": {
    "build": "echo '[build] Cloudflare detected npm run build → running wrangler deploy instead' && npx wrangler deploy --keep-vars",
    "deploy": "wrangler deploy --keep-vars",
    "postinstall": "echo '[postinstall] deps ready for wrangler deploy'",
    "dev": "wrangler dev"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "^4.45.0"
  }
}
